// The store is wired into application state during Task 6.4. Compile and test
// its complete native backend now without registering placeholder commands.
#[allow(dead_code)]
pub(crate) mod credentials;
pub(crate) mod persistence;
